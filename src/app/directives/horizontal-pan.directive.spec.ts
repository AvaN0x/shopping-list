import { HorizontalPanDirective } from './horizontal-pan.directive';

describe('HorizontalPanDirective', () => {
  it('should create an instance', () => {
    const directive = new HorizontalPanDirective();
    expect(directive).toBeTruthy();
  });
});
